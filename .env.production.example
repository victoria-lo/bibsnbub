# Production environment example
# Copy to .env.production (for CLI) or configure these in your hosting platform.
# For secrets in local files, prefer creating .env.production.local (git-ignored) to store sensitive keys.
NODE_ENV=production

# --- App / Auth ---
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_xxx
CLERK_SECRET_KEY=sk_live_xxx
# Public site URL
# NEXT_PUBLIC_APP_URL=https://your-domain.com

# --- Database (required in production) ---
# Example: Supabase connection string
# Replace YOUR_PASSWORD and db hash/host accordingly.
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.YOUR_HASH.supabase.co:5432/postgres

# --- Supabase client ---
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# --- Sentry / Monitoring (optional) ---
# Public DSN for client-side reporting
NEXT_PUBLIC_SENTRY_DSN=
# Auth token for build-time uploading of source maps, etc.
# SENTRY_AUTH_TOKEN=

# --- Arcjet & Logging (optional) ---
# ARCJET_KEY=ajkey_xxx
# LOGTAIL_SOURCE_TOKEN=xxx

# --- OneMap API (optional) ---
# ONEMAP_API_KEY=your_onemap_api_key
